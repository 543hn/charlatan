#!/bin/bash

# if see inject file, read and check for changes, else, init injects
echo "injects queued (not really)"
echo -n "" > data/injects

# same thing w announcements
echo "input announcement: (not really)"
echo -n "" > data/announcements
